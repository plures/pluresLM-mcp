// Stub: @plures/pluresdb is an optional peer dependency (native NAPI binary).
// These minimal declarations allow compilation in CI without the binary.
export declare class PluresDatabase {
  constructor(...args: any[]);
  [key: string]: any;
}
export type VectorSearchItem = any;
export type NativePluresDatabase = any;
